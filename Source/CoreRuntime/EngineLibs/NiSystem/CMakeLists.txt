set(MOD_NAME NiSystem)
set(MOD_GB_LIBS efd)
set(MOD_PCH NiSystemPCH.h)
set(MOD_INCLUDES "${CMAKE_CURRENT_LIST_DIR}/..;${CMAKE_CURRENT_LIST_DIR}")
set(MOD_DEFINES
    $<$<CONFIG:DEBUG>:NIDEBUG>;
    $<$<CONFIG:RELEASE>:NIRELEASE>;
    $<$<CONFIG:SHIPPING>:NISHIPPING>;
)

generate_project()
